# Development and CI tooling.
#
# pytest-homeassistant-custom-component is the source of truth for the test
# stack: it pins homeassistant, pytest, pytest-asyncio, pytest-cov and coverage
# to the exact versions the targeted Home Assistant release is tested against.
# Do NOT add separate pytest* pins here -- they will conflict with it.
pytest-homeassistant-custom-component==0.13.364
requests-mock>=1.12.1
mypy>=2.3.1
types-requests
ruff==0.16.6
